# Production config for Railway deployment
# Uses relative paths for container deployment

ENV: prod
DBNAME: lute.db

# Relative path for data storage (relative to /app/lute/config/)
DATAPATH: /app/data
BACKUP_PATH: /app/data/backups

# Docker mode
IS_DOCKER: true