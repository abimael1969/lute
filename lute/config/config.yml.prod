# Production config for Railway deployment
# Railway provides persistent disk storage via volumes

ENV: prod
DBNAME: lute.db

# Railway provides persistent storage through volumes
# Data will be stored in the mounted volume
# DATAPATH and BACKUP_PATH will use the default locations

# Note: Make sure to add a Railway volume mount point for persistent data
# The app will use platformdirs to determine the correct data directory