# Railway Environment Variables for Lute
# Copy this file to .env and fill in your values

# Application Environment
ENV=prod

# Optional: Custom config file path (if needed)
# CONFIG_PATH=/app/config/custom.yml

# Optional: Port (Railway sets PORT automatically)
# PORT=5001

# Optional: Enable debug mode (NEVER in production!)
# DEBUG=false