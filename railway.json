{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "python -m pip install --upgrade pip && python -m pip install flit && python -m flit install --deps production"
  },
  "deploy": {
    "startCommand": "python -m lute.main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}